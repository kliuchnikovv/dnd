package core

// Version — версия правил ядра. Реплей журнала действий сверяет её со
// снятой в прогоне (ADR-0002): правка правил меняет исход прошлых бросков, и
// такое расхождение обязано быть явной ошибкой, а не молчаливым дрейфом.
//
// Поднимается руками при изменении правил разрешения ходов.
const Version = "core-1"
