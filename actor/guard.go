package actor

import (
	"context"
	"encoding/json"
	"fmt"
	"strings"

	"github.com/kliuchnikovv/dnd/llm"
)

// Guard — проверка реплики на утечку дела. Задача проще генерации: сверить
// одну короткую фразу с коротким списком материала, и потому её можно отдать
// дешёвой модели.
//
// Порог сдвинут сознательно. Прежняя формулировка «выдумка — любой новый факт
// о мире, даже мелкий» отвергала «спину сорвал» так же, как выдуманного
// фермера Олсена, и отвергнутая реплика падала в канцелярскую заглушку. Цена
// ошибок не симметрична: пропущенная бытовая подробность не стоит ничего,
// зарубленная — стоит игроку живой речи. Поэтому в сомнении реплика проходит,
// а ловится ровно утечка ДЕЛА.
type Guard struct {
	gw     *llm.Gateway
	schema string
}

const guardPrompt = `Ты проверяешь одну реплику персонажа детективной игры на УТЕЧКУ ДЕЛА.

Ниже материал — всё, что персонаж вправе знать: его быт и посёлок, обстановка,
уже решённое о мире, факты дела, известные игрокам.

Останавливать надо две вещи, и обе — авторство, которого у персонажа нет.

1. УТЕЧКА ДЕЛА: конкретика про преступление вне материала — имя причастного,
   улика, место или время события, число вокруг преступления, вывод о том, кто
   это сделал. Дело пишет его автор.

   Сюда же — СОДЕРЖАНИЕ ЛЮБОЙ ЗАПИСИ: что помечено в книге, журнале,
   ведомости, гроссбухе, описи, чьём-то письме. Записи в этом мире читают
   игроки, а не пересказывают персонажи: если этой записи нет в материале,
   персонаж её содержания знать не может, даже когда речь о приливах и
   прочем обыденном.

2. НОВЫЙ ОБИТАТЕЛЬ МИРА: назван по имени или по должности человек либо
   учреждение, которых нет в материале, — и неважно, про дело это сказано или
   вскользь, между прочим, как бытовая мелочь. «Фермер Олсен с того берега»,
   «магистратский следователь», «наш аптекарь» — утечка, даже если звучит
   безобидно. Мир расширяет только Мастер, и делает это отдельно.

Считается утечкой и привязка к ночи события: «в ту ночь», «накануне», «перед
отливом» — про эту ночь персонаж утверждать ничего не вправе.

НЕ утечка, и это важнее всего остального:
- бытовая краска: погода, сырость, спина, остывший чай, промокшие сапоги,
  усталость, цены, работа, ворчание на начальство;
- эмоция, грубость, шутка, вежливость, обращение к собеседнику;
- уклонение, отказ, встречный вопрос, «не знаю», «не моё дело»;
- пересказ материала другими словами, в том числе неточный;
- разговор о себе и своём дне, даже с подробностями, которых в материале
  дословно нет, — если они БЕЗЛИЧНЫЕ: без новых имён, должностей, чисел и без
  привязки к ночи события;
- упоминание тех, кто есть в материале, — их называть можно.

Сомневаешься между безличной бытовой подробностью и утечкой — это краска.
Ложное срабатывание стоит игроку живой речи, и пропустить сырость, спину или
остывший чай дешевле, чем зарубить их. Но названное имя сомнением не бывает:
его либо есть в материале, либо нет.

Верни {"invented": true|false, "what": "что именно про дело придумано"}.
В what назови только придуманное — коротко, чтобы персонаж мог сказать то же
без него.`

func NewGuard(gw *llm.Gateway) *Guard {
	schema := map[string]any{
		"type":                 "object",
		"additionalProperties": false,
		"required":             []string{"invented"},
		"properties": map[string]any{
			"invented": map[string]any{"type": "boolean"},
			"what":     map[string]any{"type": "string"},
		},
	}
	raw, err := json.Marshal(schema)
	if err != nil {
		panic(err)
	}
	return &Guard{gw: gw, schema: string(raw)}
}

// Check судит реплику. Возвращает вердикт: можно ли показывать, и если нет —
// что именно придумано. «Что» нужно ремонту: без него остаётся только
// заглушка, а она стоит игроку голоса персонажа.
func (g *Guard) Check(ctx context.Context, line string, material []string, req llm.Request) (Verdict, error) {
	req.Role = llm.RoleCanonGuard
	req.Tier = g.tier()
	req.Schema = g.schema
	req.System = guardPrompt
	req.MaxTokens = 200

	var b strings.Builder
	b.WriteString("Материал:\n")
	for _, m := range material {
		if strings.TrimSpace(m) != "" {
			b.WriteString("  - " + m + "\n")
		}
	}
	b.WriteString("\nРеплика: " + line + "\n")
	req.Input = b.String()

	resp, err := g.gw.Do(ctx, req)
	if err != nil {
		return Verdict{}, err
	}
	var out struct {
		Invented bool   `json:"invented"`
		What     string `json:"what"`
	}
	if err := json.Unmarshal([]byte(resp.Text), &out); err != nil {
		return Verdict{}, fmt.Errorf("actor: проверка не разобралась: %w", err)
	}
	return Verdict{OK: !out.Invented, What: strings.TrimSpace(out.What)}, nil
}

// tier — основной. Проверка ничего не сочиняет, и дешёвая модель ей просилась
// по бюджету, но измерение на размеченном наборе (guard_corpus_test.go) это
// опровергло: дешёвый судья пропускает одну утечку из десяти, основной — ни
// одной, и ложных срабатываний нет ни у того, ни у другого.
//
// Цена ошибок несимметрична. Утечка бьёт в решаемость by-design: персонаж
// пишет в канон намёком то, что должен был выдать гейт. Разница в цене —
// порядка $0.002 за реплику, и она этого стоит.
func (g *Guard) tier() llm.Tier { return llm.TierMain }
